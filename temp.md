## 第一页面需求

1. wallhaven 顺序调整到最上面，并且默认选中
2. 上面"可选来源",改成"壁纸源",可用源为绿色，不可用的灰色，wallhaven 一直可用，其他的需要配置 API Key 才是可用
3. 按 e 键位，弹出窗口编辑或者新增对应的 API Key。

## 第二页面需求

1. 筛选条件每个源应该都不一样，根据文档 docs/ 下面的分别设置。并且除了中文翻译以外要把原始的参数名显示出来。
2. 筛选条件还需要合理的默认值，并且如果说是枚举，应该是选择而不是输入框，就算是输入框应该也要有占位符提示怎么输入
3. 下一步选项去掉，因为使用非编辑状态下的 Enter 确认来进行下一步。

## 命令行帮助需求

1. 命令行帮助说明，不同的源应该能进一步 help source ，然后把详细的筛选条件参数输出出来

## 键位与底部帮助提示需求

1. 每个页面都要有当前页面的键位帮助提示
2. 上下键位、j/k键位、tab 键位用来切换，帮助提示里显示上下键位就行; 1、2 页面用到了
3. e 键位用来编辑；1、2页面用到了，1用来配置 API Key，2用来编辑参数值
4. Enter 键位用来确认和下一步；1、2、3页面都用到了，1用来确认api Key；2页面 编辑状态用来确认编辑，非编辑状态用来下一步确认；3页面用来确认下载
5. Esc 键位用来 退出/上一步/取消编辑；1页面用来取消 API Key 编辑和退出；2页面用来取消参数编辑和上一步；3页面用来上一步
6. q 键位绑定去掉

## BUG FIX
1. 第二页最左侧小箭头，只有选到下一步的时候能看到，其他的选项看不到，可以看下选择源的时候左侧的小箭头是怎么显示的，修复一下。
